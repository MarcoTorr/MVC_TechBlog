{{> navbar }}
<div style="backgroundColor: red; height:50px">
<section class="container">
  <section class="justify-center">
    {{#each posts as |entry|}}
    {{> post-details}}
    {{/each}}
  </section>
</section>
</div>
